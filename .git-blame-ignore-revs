# Revisions listed here are skipped by `git blame` when the user has
# `blame.ignoreRevsFile` set to this file. Enable per-clone with:
#
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Add the SHA of large mechanical reformat commits below, one per line,
# with a short comment.

# Project-wide reformat: BSD/Allman braces, 120-column limit (clang-format).
710698abd3b3af248f9b3ab3a1972db9f10e6b9e
