name: Compile single headers
on:
  push:
    branches:
      - master
  pull_request:
concurrency:
  # One in-flight run per workflow per ref: a new push supersedes the
  # previous run instead of queueing behind it.  The dev host has a
  # single quantlib-dev runner, and GitHub cancels runs that sit queued
  # for 24 hours -- which is how master's post-merge validation was lost.
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  headers:
    runs-on: [self-hosted, Linux, X64, quantlib-dev]
    env:
      PATH: /opt/rh/gcc-toolset-15/root/usr/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
      LD_LIBRARY_PATH: /opt/rh/gcc-toolset-15/root/usr/lib64
      CC: /opt/rh/gcc-toolset-15/root/usr/bin/gcc
      CXX: /opt/rh/gcc-toolset-15/root/usr/bin/g++
    steps:
    - uses: actions/checkout@v6
    - name: Cache
      uses: hendrikmuhs/ccache-action@v1.2
      with:
        key: single-headers-${{ github.ref }}
        restore-keys: |
          single-headers-${{ github.ref }}
          single-headers-refs/heads/master
          single-headers-
    - name: Check
      env:
        CXX: ccache g++
      run: |
        ./tools/check_all_headers.sh
